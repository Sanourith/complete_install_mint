- name: Restart NetworkManager
  ansible.builtin.systemd:
    name: NetworkManager
    state: restarted
  become: true

- name: Activate new speed limit
  ansible.builtin.systemd:
    daemon_reload: true
    name: force-ethernet-speed.service
    enabled: true
    state: started
  become: true
